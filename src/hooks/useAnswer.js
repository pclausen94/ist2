// const useAnswer = (gameword = "Hello") => {
//   const [state, setState] = useState({
//     word: gameword,
//     letters: gameword.split("").map((letter) => ({
//       value: letter,
//       guessed: false,
//     })),
//   });

//   const result = state.letters.map((letter, index) => {
//     const letterValue = letter.guessed ? letter.value : "_";
//     return letterValue;
//   });
// };
